{
  "name": "wait-website",
  "version": "0.1.0",
  "private": true,
  "description": "Static site for WSART/WAIT with Eleventy build",
  "scripts": {
    "build": "eleventy",
    "dev": "eleventy --serve --quiet --port=8080"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}

